-r requirements.txt

# Required for EmailStr (pydantic[email])
email-validator>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# Linting & Formatting
ruff>=0.8.0

# Test HTTP client (FastAPI TestClient)
httpx>=0.27.0
