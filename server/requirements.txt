# FastAPI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sse-starlette>=2.0.0
python-multipart>=0.0.12

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# LLM
google-genai>=1.0.0
openai>=1.50.0

# RAG / Embedding
# 임베딩 스택 버전 핀 — Dockerfile의 torch(CPU wheel)와 호환 필수.
# 미핀 시 최신 transformers가 torch.float8_e8m0fnu(torch 2.7+) 참조 →
# torch 미스매치로 PreTrainedModel import 전체 붕괴 (rev31 챗봇/RAG 장애).
chromadb>=1.0.0
transformers==4.57.1
sentence-transformers==5.3.0

# HTTP / 외부 API
httpx>=0.27.0

# Email
aiosmtplib>=3.0.0

# AWS
boto3>=1.35.0

# Rate Limiting
slowapi>=0.1.9

# 설정
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
