# Python
__pycache__/
*.pyc
*.pyo
.env
.venv/
.venv311/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Expo prebuild 결과물 (plugins로 관리)
/app/android/
/app/ios/

# ChromaDB 데이터 (절대 커밋 금지)
/chroma-data/
/chroma_data/
*.chroma

# Claude Code 내부 상태 파일
.omc/
server/.omc/

# Node / Expo
node_modules/
.expo/
dist/
*.jks
*.keystore
.env.local

# 환경변수 (절대 커밋 금지)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Claude Code (로컬 설정)
.claude/

# 로컬 설계 메모 / 브레인스토밍 산출물 (커밋 금지)
docs/specs/

# mlops 파이프라인 로컬 산출물 (재생성 가능 — A/B test 산출물 포함)
mlops/data/chroma-data/
mlops/data/*.jsonl
mlops/data/*.jsonl.gz
mlops/data/chunks/
mlops/data/emb_*/
# A/B eval 리포트는 큰 산출물 — README는 보존
mlops/eval/reports/*.md
!mlops/eval/reports/README.md
app/package-lock.json
